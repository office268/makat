Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52

# ריבוי לקוחות, הרשאות ומיגרציות
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-WTF==1.3.0

# פרודקשן
gunicorn==23.0.0
psycopg[binary]==3.2.3

# זיהוי חלק מתמונה (אופציונלי - עובד גם בלעדיו)
anthropic>=0.40

# יצירת אייקוני PWA (scripts/make_icons.py)
Pillow==12.3.0

# שליפה חיה מקטלוג. Laximo ו-TecDoc בונים את התוצאה ב-JavaScript,
# ולכן בלי דפדפן אמיתי מתקבל מהם שלד ריק. כאן ולא בקובץ נפרד, כי
# בלי חשבון API זה המסלול היחיד שיש - ותלות שאפשר לשכוח להתקין היא
# פיצ'ר שכבוי בפרודקשן בלי שאיש שם לב.
# הספרייה כאן, אבל את הדפדפן עצמו צריך להוריד בבנייה:
# playwright install --with-deps chromium   (ראה README, "פריסה ל-Railway")
playwright==1.62.0

# בדיקות
pytest==9.1.1
